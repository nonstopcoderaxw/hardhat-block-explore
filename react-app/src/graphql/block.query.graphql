query block($number: ID!) {
  block(number: $number) {
    number
    baseFeePerGas
    difficulty
    extraData
    gasLimit
    gasUsed
    hash
    miner
    nonce
    parentHash
    timestamp
    transactions {
      hash
      from
    }
  }
}